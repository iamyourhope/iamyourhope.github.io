document.addEventListener('DOMContentLoaded', () => {
    let scene = document.querySelector('a-scene');
    let splash = document.querySelector('.arjs-loader');
    /* scene.addEventListener('loaded', (e) => {
        splash.style.display = 'none';
    }); */
});